qat.plugins.GateRewriter
========================

A plugin replacing a quantum gate by an equivalent subcircuit

.. warning::

    This plugin stays in this library for backward compatiblity. The plugin can be replace by :class:`~qat.plugins.KAKCompression` plugin
    of by :class:`~qat.pbo.GraphCircuit`

.. autoclass:: qat.plugins.GateRewriter
    :members: compile
